attribute vec4 vertex;
attribute vec4 color;
attribute vec2 texcoord;


varying vec4 color;
varying vec2 texcoord;
varying vec3 worldPos;

main(){
	worldPos = mul(ObjectToWorld, vertex);
	gl_Position = ObjectToClipPos(vertex);
	texcoord = texcoord;
	color = color;
	return;//gl_Position is set

}
